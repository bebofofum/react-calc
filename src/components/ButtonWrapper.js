import React from 'react';
import '../styles/ButtonWrapper.css';

const ButtonWrapper = ({children}) => {
    return (
        <div className="button-container">
            <p>I'll be the button wrapper</p>
        </div>
    )
}

export default ButtonWrapper